server {
    listen 80;
    server_name khdownloader.xyz www.khdownloader.xyz;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name khdownloader.xyz www.khdownloader.xyz;

    ssl_certificate /etc/letsencrypt/live/khdownloader.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/khdownloader.xyz/privkey.pem;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/public;

    client_max_body_size 50m;

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        fastcgi_param PATH_INFO $fastcgi_path_info;

        # Streaming - disable ALL buffering layers
        fastcgi_buffering off;
        fastcgi_request_buffering off;
        fastcgi_read_timeout 3600;
        fastcgi_send_timeout 3600;

        # Prevent nginx from buffering the FastCGI response
        fastcgi_buffer_size 4k;
        fastcgi_buffers 4 4k;

        # Disable gzip for streaming responses
        gzip off;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
